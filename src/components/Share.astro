---
/**
 * Share.astro
 * Replaces _includes/share.html
 */
import CopyButton from '@components/tools/CopyButton.svelte';
import Fa from 'svelte-fa';
import { faLink, faCheck } from '@fortawesome/free-solid-svg-icons';
import { faTwitter, faFacebook, faReddit, faLinkedin } from '@fortawesome/free-brands-svg-icons';
interface Props {
  title: string;
  url: string;
  btnClass?: string;
  labelClass?: string;
}
const { title, url, btnClass = 'btn-outline-secondary', labelClass = '' } = Astro.props;
const encodedUrl = encodeURIComponent(url);
const encodedTitle = encodeURIComponent(title);
---
<div class="share-section d-flex align-items-center flex-wrap gap-2">
  <span class={`share-label small ${labelClass}`}>Share:</span>
  <a
    class={`btn btn-sm ${btnClass}`}
    href={`https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedTitle}`}
    target="_blank" rel="noopener noreferrer"
    data-share-method="twitter"
    aria-label="Share on Twitter/X"
  >
    <Fa icon={faTwitter} />
  </a>
  <a
    class={`btn btn-sm ${btnClass}`}
    href={`https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`}
    target="_blank" rel="noopener noreferrer"
    data-share-method="facebook"
    aria-label="Share on Facebook"
  >
    <Fa icon={faFacebook} />
  </a>
  <a
    class={`btn btn-sm ${btnClass}`}
    href={`https://www.reddit.com/submit?url=${encodedUrl}&title=${encodedTitle}`}
    target="_blank" rel="noopener noreferrer"
    data-share-method="reddit"
    aria-label="Share on Reddit"
  >
    <Fa icon={faReddit} />
  </a>
  <a
    class={`btn btn-sm ${btnClass}`}
    href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodedUrl}`}
    target="_blank" rel="noopener noreferrer"
    data-share-method="linkedin"
    aria-label="Share on LinkedIn"
  >
    <Fa icon={faLinkedin} />
  </a>
  <CopyButton
    client:load
    id="copy-post-link"
    className={`btn btn-sm ${btnClass}`}
    aria-label="Copy link"
    title="Copy link"
    value={url}
    icon={faLink}
    copiedIcon={faCheck}
  />
</div>
