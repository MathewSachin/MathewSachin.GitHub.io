---
import { CAPTURA_DONATIONS } from '@data/captura_donations';
---

<table class="table table-striped table-bordered shadow">
    {CAPTURA_DONATIONS.map(donation => (
      <tr>
        <td>{donation.name}</td>
        <td>{donation.amount}</td>
      </tr>
    ))}
</table>