---
import { getCapturaDonations } from '@data/index';

const donations = getCapturaDonations();
---

<table class="table table-striped table-bordered shadow">
    {donations.map(donation => (
      <tr>
        <td>{donation.name}</td>
        <td>{donation.amount}</td>
      </tr>
    ))}
</table>