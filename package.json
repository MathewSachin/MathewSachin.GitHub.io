{
  "name": "mathewsachin-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "build-search-index": "node scripts/build-search-index.mjs",
    "bundle-orama": "esbuild --bundle --format=esm --minify --outfile=scripts/orama.js node_modules/@orama/orama/dist/browser/index.js",
    "build": "npm run bundle-orama && npm run build-search-index"
  },
  "dependencies": {
    "@orama/orama": "^3.1.18",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@pintora/standalone": "^0.8.1",
    "esbuild": "^0.27.4",
    "jsdom": "^26.1.0"
  }
}
